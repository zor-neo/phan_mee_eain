<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Demo Seed Passwords
    |--------------------------------------------------------------------------
    |
    | These values are used only when controlled demo seed commands create or
    | intentionally repair demo accounts. Keep real values in environment
    | variables and never commit them.
    |
    */

    'superadmin_password' => env('SUPERADMIN_PASSWORD'),
    'author_password' => env('DEMO_AUTHOR_PASSWORD'),

];
